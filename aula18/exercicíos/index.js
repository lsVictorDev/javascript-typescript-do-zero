// Exercício básico usando função
// Faça uma função que soma dois números

function soma(x, y){
    resultado = x + y;
    return(resultado);
}

console.log(soma(5, 10)); // Mostra 15
// caso eu deixasse o valor da soma no console.log vazio, resultaria em um NaN

// Gabarito

function soma(x, y){
    resultado = x + y;
    return(resultado);
}

console.log(soma(1, 1));